import { Button } from "./Button";

export default {
  title: "Trawellor components/Button",
  component: Button,
}

const Template = (args) => <Button {...args} />;
export const Icon_Button = Template.bind({})
