import { RoundButton } from "./RoundButton";

export default {
  title: "Trawellor components/Button",
  component: RoundButton,
}

const Template = (args) => <RoundButton {...args} />;
export const Round_Button = Template.bind({})
