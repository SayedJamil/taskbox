import { BottomNavbar } from "./BottomNavbar";

export default {
    title: "Trawellor components/Navbar",
    component: BottomNavbar,
}

const Template12 = (args) => <BottomNavbar {...args} />;
export const Bottom_Navbar = Template12.bind({})
